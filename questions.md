### <div style="background-color:rgb(230, 137, 75)">1. 数据库的内联存储什么意思？</div>
- 数据库的“内联存储”指的是将数据值直接存储在记录（tuple 或行）的数据结构内部，而不是在该记录之外额外的存储区域中保存数据的指针。
#### 基本概念
- **内联存储(Inline Storage)**：数据直接放在记录本身内部，占据记录固定的一部分空间。这意味着，当记录被读取时，内联存储的数据可以和记录的其他字段一起被加载到内存中，无需额外的寻址或 I/O 操作。
- **外联存储(Out-of-Line Storage)**: 当数据量较大或不适合直接存储在行内时，数据库可能只在行内存储一个指针或引用，将实际的数据存储在另一个位置。这样可以避免行变得过大，但会带来额外的 I/O 以及间接寻址开销。
##### 内联存储的应用实例
- **固定或小型数据类型**
  - 对于固定长度的数据类型（例如 INTEGER、CHAR 等）或较短的可变长度类型（例如短的 VARCHAR），数据库通常会采用内联存储，这样数据存储在一行内部。
- **混合存储策略**
  -  对于大文本或 BLOB 数据，为了避免影响记录整体大小，数据库往往采用混合策略：如果数据较小，则内联存储；如果超出一定阈值，则转为外联存储，在记录中只存放一个指针。
##### 非内联存储应用实例
- **大对象或大字段**
  - 当某个字段的数据量超过了数据库为内联存储预设的阈值时（例如，长文本、BLOB、CLOB 等大型数据类型），就会将数据存放在独立的存储区中，而在主记录中只保留一个指针。这种设计可以避免将单条记录变得过大，进而影响I/O效率和内存缓存命中率。
- **可变长度字段**
  - 对于可变长度数据类型（比如 VARCHAR 类型），数据库管理系统（DBMS）往往会设定一个长度上限。如果某些数据值的长度超过了这个上限，或者出于节省空间和提高更新性能的考虑，也会采用非内联存储，只在记录中存储一个引用或偏移量，而实际数据则存储在外部的位置。
- **更新频繁且内容较大的字段**
  - 在某些设计中，大对象如果被频繁更新，将数据放在单独的存储区域可以减少对主记录的频繁修改和重写。这种设计有助于减少更新数据时可能引起的数据页拆分或重组操作，从而提高性能。
- **数据库引擎设计策略**
  - 有些数据库系统为了提高存储空间的利用率或降低内存占用，会对某些类型的数据默认采用非内联存储。例如，系统可能在检测到数据不足以填满一定内存页或连续存储区时，自动将其放到外部区域，再在记录中存储其位置引用。

